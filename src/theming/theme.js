import { createMuiTheme } from '@material-ui/core/styles';

export const theme = createMuiTheme({
  palette: {
    primary: {
      light: '#84c887',
      main: '#66bb6a',
      dark: '#47824a',
      contrastText: '#fff',
    },
    secondary: {
      light: '#84c887',
      main: '#00acc1',
      dark: '#47824a',
      contrastText: '#000',
    },
  },
});
